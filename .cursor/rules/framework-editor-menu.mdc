---
description: 自研 Editor 扩展的菜单项统一挂在 Tools 下
globs: Assets/**/Editor/**/*.cs
alwaysApply: false
---

# Editor 菜单规范

## 强制约定

项目**自研**的 Unity Editor 扩展（`[MenuItem]` 顶栏入口）**必须**挂在 **`Tools/`** 下，禁止新建独立顶栏根菜单（如 `Framework/`、`Game/`、`Luban/` 等顶层）。

推荐路径格式：

```text
Tools/{模块或领域}/{具体操作}
```

## 现有示例（正确）

```csharp
[MenuItem("Tools/Luban/Generate Client Config")]
[MenuItem("Tools/YooAsset/Generate Collector")]
```

## 禁止

```csharp
// ❌ BAD — 自定义顶栏根
[MenuItem("Framework/Generate Collector")]
[MenuItem("Luban/Generate Client Config")]
[MenuItem("Game/Battle/Bake")]

// ✅ GOOD
[MenuItem("Tools/YooAsset/Generate Collector")]
[MenuItem("Tools/Luban/Generate Client Config")]
[MenuItem("Tools/Battle/Bake")]
```

## 例外

| 类型 | 约定 |
|------|------|
| `Assets/...` 资源右键菜单 | 允许（Unity 惯例），仍建议语义清晰 |
| `CreateAssetMenu` | 按资源创建流程，不走顶栏 `Tools/` |
| `ContextMenu`（Inspector 组件右键） | 允许，绑定组件本身 |
| 第三方插件自带菜单 | 不改动 |

## Agent 行为

- 新增/修改 `[MenuItem]` 时，默认使用 `Tools/{模块}/...`
- Validate 菜单（`[MenuItem(path, true)]`）必须与执行菜单使用**同一路径**
- 发现顶栏非 `Tools/` 的自研菜单 → 改为 `Tools/` 并同步更新 README / 文档中的菜单路径
